# Copia a .env y ajusta. (En OMV puedes poner estas variables dentro del compose.)
ADMIN_USER=admin
ADMIN_PASSWORD=CAMBIA-ESTA-CLAVE
TZ=America/Lima

# Avanzado (normalmente no hace falta tocar):
# RCLONE_CONF=/config/rclone/rclone.conf
# OMV_CONFIG_DIR=/host/etc-openmediavault
# COMPOSE_ROOTS=/srv/dev-disk-by-uuid-AAA/compose:/srv/dev-disk-by-uuid-BBB/compose
# LOCAL_DEST_DIR=/destino-local
# MAX_FILE_MB=50
